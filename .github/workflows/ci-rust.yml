name: CI Rust

on:
  push:
    branches: [main]
    paths:
      - 'rust-backend/**'
      - 'lyremember-app/src-tauri/**'
      - '.github/workflows/ci-rust.yml'
  pull_request:
    branches: [main]
    paths:
      - 'rust-backend/**'
      - 'lyremember-app/src-tauri/**'
      - '.github/workflows/ci-rust.yml'

defaults:
  run:
    working-directory: rust-backend

jobs:
  check:
    name: Rust Check & Clippy
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust-backend

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-dev libsqlite3-dev

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Cargo check
        run: cargo check

      - name: Clippy
        run: cargo clippy -- -D warnings

      - name: Tests
        run: cargo test

  fmt-tauri:
    name: Tauri Rust Format Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: lyremember-app/src-tauri

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Format check
        run: cargo fmt --all -- --check
