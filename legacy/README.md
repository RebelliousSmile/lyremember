# Legacy

Code archivé qui n'est plus la stack canonique du projet mais reste utile
comme référence ou historique.

## `python-cli/`

Proof of concept initial : CLI Python (Click + storage JSON + modes de
pratique fill-blank/flashcard/line-by-line). Conservé pour
référence et tests.

```bash
cd legacy/python-cli
pip install -r requirements.txt pytest
python -m pytest tests/ -v
```

Voir aussi : la section "Legacy Python CLI Documentation" du
[README racine](../README.md).
